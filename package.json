{
  "name": "seekreap-tier0-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd src && node server.js",
    "install": "cd src && npm install",
    "test": "cd src && npm test"
  }
}
