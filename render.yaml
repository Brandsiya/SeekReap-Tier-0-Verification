services:
  - type: web
    name: seekreap-tier0-verification
    env: node
    buildCommand: npm install
    startCommand: node cli.js --version
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
    branch: master
